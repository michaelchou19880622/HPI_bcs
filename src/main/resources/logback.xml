<?xml version="1.0" encoding="UTF-8"?>

<configuration scan="true">
	<property name="APP" value="bcs" />
	<property name="LOG_HOME" value="/bcs/log/richart/richmenu" />
	<property name="CHARSET" value="UTF-8" />
	<property name="PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} %X{ServiceId} [%L] - %msg%n" />
	<!-- 控制台輸出 -->
	<appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoder默認配置為PartternLayoutEncoder    -->
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${PATTERN}</pattern>
            <charset>${CHARSET}</charset>
        </encoder>
    </appender>
    <!-- 按照每天生成日誌文件 -->
    <appender name="ROLLINGFILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!--日誌文件輸出的文件名-->
            <FileNamePattern>${LOG_HOME}/${APP}/${APP}-%d{yyyy-MM-dd}-%i.log</FileNamePattern>
            <!--日誌文件保留天數-->
            <MaxHistory>14</MaxHistory>
            <maxFileSize>10MB</maxFileSize>    
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${PATTERN}</pattern>
            <charset>${CHARSET}</charset>
        </encoder>
    </appender>

	<root level="INFO">
		<appender-ref ref="ROLLINGFILE" />
		<appender-ref ref="CONSOLE" />
	</root>
</configuration>